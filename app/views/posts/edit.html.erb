<h1>id: <%= @post.id %> のNEWS編集ページ</h1>

<%= form_with(model: @post, local: true) do |f| %>
  <%= f.label :title, 'タイトル' %>
  <%= f.text_field :title %>
  <%= f.label :text, '本文' %>
  <%= f.text_field :text %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', admins_path %>